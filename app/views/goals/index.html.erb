<h1>Goals</h1>
<ul>
<% @goals.each do |goal| %>
  <li><%= link_to "#{goal.title}", goal_url(goal) %> by <%= goal.user.username%></li>
<% end %>
</ul>

<%= link_to "New Goal", new_goal_url %>
<%= link_to "#{current_user.username}'s profile", user_url(current_user) %>
